# Direct user-authorized continuation (2026-09-08 14:01 UTC onward)

## Direct resumed execution — 2026-09-09 (supersedes older live status)

Human explicitly asked to execute all remaining tests again. This direct turn
resumed mutations with individually reviewed bounded commands, not via the paused
read-only heartbeat. Read RESULTS-resumed-campaign.md first.

Completed: actual open-request duplicate response and approval at5415 both
reverted without changing5/6; subsequent fault-disabled fresh requests all6/6
finalized/active; all6 accepted block producers in5503..5582. Node4/5/6 won9/10/14.
Component valid-verification load24runs completed04:44:06 UTC,8,415,940 operations,
zero failures; source/results archived and checked.

Actual network workload also completed05:03:22 UTC:216bounded valid-static
evidence/wrong-identity admissions at1/4/8requests/s in3sequential repeats.
All216begin receipts1,0challenges/0approvals/inactive, with producer statement
mismatch logs. Archive downloaded and hash checked. Both verifier and network
load scopes/limitations are documented; full-network valid admission saturation
was not established.

All six fresh-chain repetitions p256/p128/p64-r2/-r3 completed and passed all6node
header/event/root audits. The finite direct orchestrator exited successfully.
Final p64-r3 public DBs were cleanly stopped, downloaded, checked against each
node's remote digest and services restarted. All previous phase data retained.
Current phase p64-r3; no attack/load orchestrator remains running from this turn.
See RESULTS-eligibility-repeats.md. Every preceding public DB is cleanly stopped,
archived/downloaded and hash checked; original DB directories retained.
No new VMs, no private artifacts, no remote git push.
Provider DELETE2026-09-09T10:50Z unchanged.

Local verify-campaign-results.ps1 passed: six additional6node/6slot audits,
actual reorg10orphan blocks/4orphan approvals/0canonical reuse, two open-request
replay receipt observations,24valid-verifier runs with8,415,940operations and
zero failures,216expensive-admission zero-progress verdicts. PUBLIC-RESUMED.sha256
indexes80public archives; archive member names were checked for unsafe/key paths.
This is bounded functional/adversarial/performance evidence, not universal safety
or full-network legitimate new-registration capacity. Manual restart after expiry
and adaptive/time-relaxed eligibility remain explicit limitations.
Paper source, conclusion and evidence index updated; final PDF built twice and
changed pages16–20 visually checked. No Git commit/push; pre-existing edits kept.

## Historical verified status Sep8 15:22 UTC / Sep9 00:22 KST

The response-fault run is now observed, not merely prepared. All three late
requests had six challenges/responses but five approvals (slots20..24), remained
unfinalized/inactive at head206 beyond deadline84. Producer logs reject the two
wrong certification domains and the corrupted signature. All six public expired
snapshots downloaded and hash checked; see RESULTS-response-fault.md.

The 15:21 heartbeat allows only read-only cloud observation/public evidence
download and local work. No subsequent remote mutation was performed. Current
data-attack has bootstrap3 mining and late3 inactive with fault markers still
enabled. Recovery retry and active-window duplicates have NOT run. Sustained
valid verification load/saturation and independent repetitions also remain.
Local result organization and paper update/build/changed-page review are done;
the restricted heartbeat is to be paused, not reported as campaign completion.
Provider DELETE remains 2026-09-09 10:50 UTC. Historical steps below are not
authorization to execute remote mutations from this heartbeat.

## Historical status 15:03 UTC / Sep9 00:03 KST

p64 and actual reorg completed and preserved. See RESULTS-eligibility.md and
RESULTS-reorg.md. All six phase DB backups downloaded and checked. Reorg displaced
10 blocks, four old approval events do not recur canonically; three late requests
completed on canonical chain; common height64 state/hash agrees all6.

Current live phase: data-attack, research response-fault binary SHA256
7d35dd45a9311ea436a616d8b24167eacd0568ac5795d85ca8a0456191dac637.
Linux normal and enrollmentresearch tpmenroll/tpmproducer tests passed before
build. Normal builds compile no-op outbound fault hooks. Producer verifier and
quorum unchanged. Attack phase genesis hash in install log is b856f3..a04dbb;
public genesis file SHA256087c547101d10e45019f63b2b6a32090dba7dbcb6d2b11745a195c4380db4a03.
Bootstrap3 mining started15:01:38–41. Late3 started15:03:12–14 AFTER block9.
Explicit response-fault.enabled marker on nodes4–6: first slot ONLY produces
wrong-request domain (n4), wrong-chain domain (n5), corrupted Certify signature
(n6). All later slots normal. Expected5/6 is only an expectation, NOT yet a result.
Observe actual producer rejection, on-chain response/approval counts and expiry.
Then preserve before renaming marker to disabled and retry fresh registration.
Do not use phase install again: data-attack already exists and is running.

Historical replay binary node1 supports -event ProducerSlotApproved. Updated
replay-linux uploaded to node6 (session37079 may need poll); use -event
ProducerResponseSubmitted. Once invalid requests have stable5/6 and still open,
run one actual duplicate response and one duplicate approval (fresh unit names)
to test active-window duplicate guards, not only historical closed requests.
Wait for stable approvals to avoid interpreting legitimate concurrent changes as
replay mutations. Source remains loopback chain103994, at most one tx per event.
Pending attack phase must not be counted done yet. Other public workload72requests,
three adaptive eligibility runs, real reorg, closed-request replay all recorded.

## Latest status 14:35 UTC (supersedes live progress below)

All three bounded invalid-admission load rates 1/4/8 completed: 24 requests each,
72 zero-progress verdicts total. See RESULTS-bounded-load-replay.md. Historical
response and approval replay each broadcast and reverted, records unchanged.
Node6's missing approval fixture failed; approval retested on node1 successfully.
All six research12 clean DBs preserved/downloaded/hash checked.

p256 completed: three late requests six approvals each, finalized, all six active
and all six produced at least one canonical block in captured heights1..51.
Common height40 hash 0x10621361fca5cca3544b976a64027804a8012205f890d8e8014ae6fa290e63ae
and root 0x8471b8c44ac61950403834732391344f06c32ffa6dea6c59662d5730d846c6d9.
All public archives and clean frozen DBs downloaded and hash checked.

p128 completed registration: three late requests six approvals each, all active.
Captured heights1..64 contain five producer identities; node6 had no accepted
block in this interval. Do not report six successful block producers for p128.
Common height40 hash 0xd3dcc553daa8b0500a243db0971c92c5725c5b381efa6d0f0bde6eca7b17c64a
and root 0xc3ea13af6d94aca033ea595b5a5b612ffa32d25466a7b9ffbcf6490ae0e5859b.
All public archives downloaded; frozen DBs downloaded and checked before p64.

p64 now running: bootstrap mining started14:33:25–27, block2 confirmed; late
integrated enrollments started14:34:10–14. Last read at height17, three active.
No partition has been applied. phase-reorg.tgz prepared and uploaded all nodes
(node1 original root-owned file unchanged), SHA256
9b47d897e93757924fa9e53b7d4cc2b2895285d6ab36ebdc937cf23d792bc627.
partition-local.sh uploaded all6: dedicated iptables chain only TCP30303 cross
groups {1,4,5}/{2,3,6}, autoheal180s scheduled BEFORE drops. Not run yet.

Next: complete p64, collect/hash/download, freeze/check DBs, fresh reorg phase.
Observe common head with bootstrap3 before split; split with timers; start late
registration while split; capture BOTH branch blocks/events BEFORE autoheal;
verify actual branch replacement, restored connectivity and canonical state.
Keep incomplete proof-reuse subcases explicit. Local summarize-phase.ps1 and
summarize-load.ps1 do read-only analysis. Paper still reflects prior partial data
and needs the newly collected results after the live campaign.

User explicitly reiterated that all agreed experiments must be performed. The
active interactive turn resumed bounded injections on the existing six VMs.
This does NOT broaden the read-only heartbeat; do not let a heartbeat stop or
reinitialize services while the direct campaign is running.

At 14:01 UTC all checked services active. Measured binary and p256 public archive
hashes matched NIGHT_HANDOFF.md. No new VMs or deadline extension.

## Live progress

- worldland-bounded-load1: completed exit 0; 24 malformed enrollment starts at
  1/s, 40 second quiet control; artifacts/bounded-load-1.tgz downloaded.
- worldland-bounded-load4: started on node 6, bounded 24 requests, 420 second unit
  limit. Do not overlap another injection until it finishes.
- load8 and historical duplicate-calldata replay still pending.
- replay-linux compiled locally and uploaded to node 6, not yet executed.
- p256 public genesis + measured binary + phase scripts uploaded to nodes 2–6.
  Node 1 already has identical root-owned archive/binary; upload could not overwrite
  those two files, intentionally leave them unchanged. Script uploads succeeded.
- No service stopped or phase initialized yet.

Load1 resource fields were captured without labels. A separate systemctl show
confirmed the output order MemoryCurrent, CPUUsageNSec, TasksCurrent; later rates
use explicit field labels. Observed receipt latency is delayed by sequential
receipt polling and must not be presented as verification latency.

Next: finish load4, run load8 and replay, preserve public results; freeze each
research12 chain to a new archive (no data deletion), download/hash-check before
fresh p256/p128/p64. Reorg needs bounded automatic reconnect and proof of actual
branch replacement, not simply peer disconnection. All incomplete coverage must
remain explicit. This direct turn may request reviewed mutations individually;
do not use the previously rejected broader unattended cleanup automation.
